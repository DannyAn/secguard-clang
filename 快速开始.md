# SecGuard 快速开始

> AI 增强的 C 程序安全分析平台 — 4 层收敛管线 + AI Agent 推理

## 1. 前置条件

| 依赖 | 最低版本 | 用途 |
|------|---------|------|
| Go | 1.21+ | 构建 secguard 二进制 |
| CGo (C 编译器) | gcc/clang | tree-sitter-c 需要 CGo |
| SQLite | 内置 (modernc.org/sqlite) | 无需系统安装 |
| python3 | 3.8+ | deploy.sh 模板展开 |
| OpenCode 或 Claude Code | 最新版 | AI Agent 交互 |

检查环境：

```bash
go version          # 需要 1.21+
gcc --version       # 或 clang --version
python3 --version   # 需要 3.8+
```

## 2. 一键安装

```bash
cd secguard-lite
./deploy.sh
```

`deploy.sh` 会自动完成：

1. **构建二进制** → `~/.local/bin/secguard`
2. **安装 OpenCode 扩展** → `~/.config/opencode/extensions/zhuque-secguard/`
3. **安装 Claude Code 插件** → `~/.claude/skills/zhuque-secguard/` (skills-dir 插件，自动加载)
4. **注册全局命令** → `/secguard` 在两个平台均可用
5. **合并权限** → `~/.claude/settings.json` 自动添加 `Bash(secguard *)` 权限

### 仅安装某一平台

```bash
./deploy.sh opencode       # 仅 OpenCode
./deploy.sh claude-code    # 仅 Claude Code
./deploy.sh --no-binary    # 跳过二进制构建，仅部署扩展文件
```

### 验证安装

```bash
secguard --version
# 输出: 0.1.3

secguard --help
# 输出用法说明（已修复：现在输出到 stdout）
```

## 3. CLI 命令用法

### 3.1 index — 索引代码库

将 C 源码解析为程序语义图，存入 SQLite 数据库。

```bash
secguard index ./my-project
```

输出 JSON，包含索引的文件数、函数数、调用关系数。

### 3.2 scan — 全管线扫描

**最常用的命令。** 一次完成：索引 → 检测 → 4 层收敛 → 输出证据包。

```bash
secguard scan ./my-project
```

输出 JSON 证据包，每种漏洞类型最多 15 个候选，已按收敛管线过滤。

### 3.3 plan — 单类型收敛

对已索引的代码库，运行某一类漏洞的收敛管线：

```bash
secguard plan null-deref
secguard plan buffer-overflow
secguard plan memory-leak
secguard plan resource-leak
secguard plan uninit
secguard plan injection
```

### 3.4 status — 查看索引状态

```bash
secguard status
```

显示已索引的文件、函数、以及代码变更后的过期标记。

### 3.5 query — 运行 Skill 查询

```bash
secguard query null-deref
```

### 3.6 report — 输出所有发现

```bash
secguard report
```

从数据库读取所有已确认和疑似的漏洞发现，输出 JSON。

### 3.7 指定数据库路径

所有命令支持 `--db` 标志：

```bash
secguard scan ./my-project --db /tmp/my-analysis.db
secguard status --db /tmp/my-analysis.db
```

默认数据库路径：`./sgre.db`

## 4. OpenCode 中使用

### 4.1 通过 /secguard 命令（推荐）

在 OpenCode TUI 中输入：

```
/secguard ./my-project
```

这会触发 `security-auditor` 子代理执行完整安全分析流程。

### 4.2 通过 @security-auditor 显式调用

```
@security-auditor analyze src/ for null pointer vulnerabilities
```

### 4.3 Agent 触发机制（重要）

`security-auditor` 定义为 `mode: subagent`，意味着：

- **不会**通过自然语言自动触发（输入 "analyze code for vulnerabilities" 不会调用它）
- **会**被 `/secguard` 命令显式委派
- **会**被 `@security-auditor` 显式调用
- **会**被主 Agent 委派（如果主 Agent 的 system prompt 指示了委派规则）

调用链：

```
用户输入 /secguard ./project
       ↓
secguard 命令（commands/secguard.md）
       ↓
委派给 security-auditor 子代理
       ↓
secguard scan ./project  →  JSON 证据包
       ↓
加载匹配的 Skill（null-deref, buffer-overflow, ...）
       ↓
逐条分类：confirmed / suspected / false-positive
       ↓
输出结果表格 + 修复建议
```

### 4.4 验证 Agent 已加载

在 OpenCode TUI 中输入 `/`，查看自动补全列表中是否有 `secguard` 命令。

输入 `/agents`，查看是否有 `security-auditor` 代理。

如果未出现，检查扩展目录：

```bash
ls ~/.config/opencode/extensions/zhuque-secguard/
ls ~/.config/opencode/commands/secguard.md
```

### 4.5 OpenCode 工具权限

`security-auditor` 子代理的权限配置（`opencode.json`）：

| 权限 | 值 | 说明 |
|------|-----|------|
| edit | deny | 不允许修改源码 |
| bash | deny | 不允许任意 bash |
| read | allow | 可读源码 |
| grep/glob | allow | 可搜索代码 |
| secguard_* | allow | 可调用所有 secguard 工具 |

## 5. Claude Code 中使用

### 5.1 通过 /secguard 命令

在 Claude Code 中输入：

```
/secguard ./my-project
```

### 5.2 插件自动加载

Claude Code 插件安装在 `~/.claude/skills/zhuque-secguard/`，作为 skills-dir 插件**自动加载**，无需手动启用。

### 5.3 PostToolUse 钩子

安装后，每次编辑或写入源文件时，Claude Code 会提示：

```
{"note": "Source file edited — SecGuard index may be stale. Re-run /secguard to refresh."}
```

提醒你重新扫描以获取最新结果。

### 5.4 权限配置

`deploy.sh` 自动将以下权限合并到 `~/.claude/settings.json`：

```json
{
  "permissions": {
    "allow": [
      "Bash(secguard scan *)",
      "Bash(secguard index *)",
      "Bash(secguard plan *)",
      "Bash(secguard report *)",
      "Bash(secguard status *)",
      "Bash(secguard query *)"
    ]
  }
}
```

### 5.5 验证插件已加载

```bash
ls ~/.claude/skills/zhuque-secguard/.claude-plugin/plugin.json
ls ~/.claude/skills/zhuque-secguard/commands/secguard.md
ls ~/.claude/skills/zhuque-secguard/bin/secguard
```

## 6. 完整使用示例

### 6.1 命令行直接使用

```bash
# 扫描 benchmark 示例
secguard scan ./examples/c-vuln-benchmark/src

# 查看索引状态
secguard status

# 单独运行某类漏洞的收敛
secguard plan null-deref

# 输出所有发现
secguard report
```

### 6.2 在 OpenCode 中使用

```
# 启动 OpenCode
opencode

# 在 TUI 中输入：
/secguard ./examples/c-vuln-benchmark/src

# security-auditor 代理会：
# 1. 运行 secguard scan
# 2. 加载 null-deref, buffer-overflow, memory-leak, injection 技能
# 3. 逐条分类证据
# 4. 输出结果表格 + 修复建议
```

### 6.3 在 Claude Code 中使用

```
# 启动 Claude Code
claude

# 输入：
/secguard ./examples/c-vuln-benchmark/src

# 或直接让 Claude 分析：
> 请用 SecGuard 扫描 ./src 目录的安全漏洞
```

## 7. 架构简述

```
C 源码
  ↓
Tree-sitter 解析 → AST
  ↓
Indexer → 程序语义图 (SQLite)
  ↓
Detectors → ~600 原始候选
  ↓
4 层收敛管线:
  L1: 去重合并
  L2: 安全函数/包装器过滤
  L3: 数据流验证
  L4: 证据打包 (≤15/类型)
  ↓
AI Agent (security-auditor)
  ↓
分类: confirmed / suspected / false-positive
  ↓
结果表格 + 修复建议
```

## 8. 故障排除

### 8.1 `secguard` 无输出

**已修复。** 之前 `--help` 和无参数时的用法说明输出到 stderr，在某些终端中不可见。现在已改为输出到 stdout。

如果仍有问题，检查二进制是否在 PATH 中：

```bash
which secguard
# 应输出: /Users/<user>/.local/bin/secguard
```

### 8.2 构建失败：CGO 相关

```bash
# 确保 CGO_ENABLED=1
CGO_ENABLED=1 go build -o ~/.local/bin/secguard ./cmd/secguard

# 如果缺少 C 编译器：
# macOS:  xcode-select --install
# Ubuntu: sudo apt install gcc
```

### 8.3 构建失败：依赖问题

```bash
# 在无网络沙箱环境中：
GONOSUMDB='*' GOFLAGS=-mod=mod go mod tidy
GONOSUMDB='*' GOFLAGS=-mod=mod CGO_ENABLED=1 go build -o ~/.local/bin/secguard ./cmd/secguard
```

### 8.4 `secguard scan` 报数据库错误

```json
{"error":"failed to open database: db: open: ping: unable to open database file (14)"}
```

原因：当前目录无写权限。解决：指定 `--db` 路径：

```bash
secguard scan ./my-project --db /tmp/sgre.db
```

### 8.5 OpenCode 中 `/secguard` 命令不出现

```bash
# 检查全局命令是否已注册
ls ~/.config/opencode/commands/secguard.md

# 如果不存在，重新部署：
./deploy.sh opencode
```

### 8.6 Claude Code 中插件未加载

```bash
# 检查插件目录
ls ~/.claude/skills/zhuque-secguard/.claude-plugin/plugin.json

# 检查 settings.json 中的权限
cat ~/.claude/settings.json | python3 -m json.tool

# 重新部署：
./deploy.sh claude-code
```

### 8.7 Agent 未被自然语言触发

这是**预期行为**。`security-auditor` 是 `mode: subagent`，不会通过自然语言自动触发。使用：

- `/secguard [path]` — 通过命令触发（推荐）
- `@security-auditor [指令]` — 显式调用子代理

### 8.8 `secguard report` 返回空数组

这是**预期行为**。`secguard scan` 产出证据包（evidence packages），但不写入 findings。findings 由 AI Agent 在分类后写入。

流程：
```
secguard scan → 证据包 JSON → AI Agent 分类 → 写入 findings → secguard report
```

如果未运行 AI Agent 分类，`secguard report` 返回 `[]`。

### 8.9 `secguard scan` 输出包含日志行

**设计行为**：日志输出到 stderr（NDJSON 格式），JSON 结果输出到 stdout。

```bash
# 仅获取 JSON 结果（丢弃日志）
secguard scan ./src 2>/dev/null

# 获取日志和结果（用于调试）
secguard scan ./src 2>&1

# 分别保存
secguard scan ./src 1>results.json 2>logs.ndjson
```

### 8.10 OpenCode Agent 模型未找到

错误：`Model not found: anthropic/claude-sonnet-4-20250514`

已修复：从 `opencode.json` 和 `security-auditor.md` 中移除了硬编码模型。Agent 现在使用 OpenCode 全局配置的模型。

如需指定模型，在 OpenCode 全局配置中设置，或在 `opencode.json` 的 agent 配置中添加 `"model": "<valid-model-name>"`。

## 9. 支持的漏洞类型

| 类型 | CLI 参数 | 检测器 | 测试用例 |
|------|---------|--------|---------|
| 空指针解引用 | `null-deref` | NullSourceDetector, DereferenceDetector, NullGuardDetector, InterproceduralDetector | TC01-TC04 |
| 内存泄漏 | `memory-leak` | MemoryLeakDetector | TC05-TC06 |
| 资源泄漏 | `resource-leak` | ResourceLeakDetector | TC07-TC10 |
| 未初始化变量 | `uninit` | UninitVariableDetector | TC11-TC17 |
| 缓冲区溢出 | `buffer-overflow` | BufferOverflowDetector | — |
| 注入 | `injection` | InjectionDetector | — |

## 10. 技能 (Skills)

AI Agent 在分类证据时加载的领域知识：

| 技能 | 路径 | 用途 |
|------|------|------|
| null-deref | `shared/skills/null-deref/SKILL.md` | 空指针分类规则 |
| buffer-overflow | `shared/skills/buffer-overflow/SKILL.md` | 缓冲区溢出分类规则 |
| memory-leak | `shared/skills/memory-leak/SKILL.md` | 内存泄漏分类规则 |
| injection | `shared/skills/injection/SKILL.md` | 注入分类规则 |

每个 SKILL.md 包含：确认条件、疑似条件、误报条件、CWE 映射。