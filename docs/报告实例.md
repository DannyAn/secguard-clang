[Finding]

Skill: memory.null_dereference

Severity: B

Confidence: high

Summary:

hrp_fastsync_check() 中 hrp_malloc 返回值未做 NULL 检查，直接传入 memset_s 并访问字段，内存耗尽时触发空指针解引用。

Location:

/usr1/code/xyz/hrp/src/mgmtplane/app/sync/file/hrp_sync_cfg.c:517

Evidence:

source: hrp_malloc(SID_CFG, sizeof(fast_sync_check_info_t)) at L517 — nullable source (wraps malloc/VOS_Malloc_F)

sink: memset_s(check_info, sizeof(fast_sync_check_info_t), 0, sizeof(fast_sync_check_info_t)) at L518 — dereferences check_info pointer

sink: check_info->buf_len = FASTSYNC_CHECK_BUFF_LEN at L519 — field access on potentially NULL pointer

path: malloc returns NULL -> L518 memset_s writes to NULL address -> deterministic SIGSEGV

condition: memory exhaustion causing malloc failure

Reasoning:

hrp_malloc 在 L517 分配内存后，L518 立即使用 memset_s 对返回指针解引用，L519 访问 buf_len 字段，中间无任何 NULL 检查。同函数中其他路径（L237-242）正确处理了 check_info 的释放，但遗漏了分配后的 NULL 守卫。同代码库中其他 hrp_malloc 调用点均有 if_null_return 守卫（如 hrp_bgp_health_get_peer L611、hrp_cmp_except_ha_send L1057），此处属于明显的遗漏。

Exception Check:

无 RAII、无 ownership transfer、无 safe wrapper。hrp_malloc 是标准 malloc 封装，返回 NULL 是合法行为。

Fix Strategy:

在 L517 后增加 NULL 检查：

```c

fast_sync_check_info_t *check_info = (fast_sync_check_info_t *)hrp_malloc(SID_CFG, sizeof(fast_sync_check_info_t));

if (check_info == NULL) {

return VOS_ERR;

}

```